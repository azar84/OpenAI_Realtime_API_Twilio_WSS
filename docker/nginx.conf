events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Logging
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;

    # Basic Settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 16M;

    # Gzip Settings
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Rate Limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=websocket:10m rate=5r/s;

    # Upstream servers
    upstream webapp {
        server localhost:3000;
        keepalive 32;
    }

    upstream websocket {
        server localhost:8081;
        keepalive 32;
    }

    # Main Server
    server {
        listen 80;
        server_name _;

        # Security Headers
        add_header X-Frame-Options DENY;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        # WebSocket Server Routes
        location /call {
            limit_req zone=websocket burst=10 nodelay;
            
            proxy_pass http://websocket;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket specific timeouts
            proxy_read_timeout 86400;
            proxy_send_timeout 86400;
            proxy_connect_timeout 60;
        }

        location /logs {
            limit_req zone=websocket burst=10 nodelay;
            
            proxy_pass http://websocket;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket specific timeouts
            proxy_read_timeout 86400;
            proxy_send_timeout 86400;
            proxy_connect_timeout 60;
        }

        # Twilio Webhook Routes
        location /twiml {
            limit_req zone=api burst=20 nodelay;
            
            proxy_pass http://websocket;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Content-Type application/xml;
        }

        location /status {
            limit_req zone=api burst=20 nodelay;
            
            proxy_pass http://websocket;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # WebSocket Server API Routes
        location /tools {
            limit_req zone=api burst=20 nodelay;
            
            proxy_pass http://websocket;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /public-url {
            limit_req zone=api burst=20 nodelay;
            
            proxy_pass http://websocket;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Working Web Application Interface
        location / {
            limit_req zone=api burst=20 nodelay;
            
            return 200 '<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenAI Realtime + Twilio Demo</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); min-height: 100vh; }
        .container { max-width: 1200px; margin: 0 auto; padding: 20px; }
        .header { text-align: center; color: white; margin-bottom: 40px; }
        .header h1 { font-size: 3rem; margin-bottom: 10px; }
        .header p { font-size: 1.2rem; opacity: 0.9; }
        .grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin-bottom: 40px; }
        .card { background: white; border-radius: 12px; padding: 30px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); transition: transform 0.3s ease; }
        .card:hover { transform: translateY(-5px); }
        .card h3 { color: #333; margin-bottom: 15px; font-size: 1.3rem; }
        .card p { color: #666; line-height: 1.6; margin-bottom: 20px; }
        .btn { display: inline-block; background: #667eea; color: white; padding: 12px 24px; text-decoration: none; border-radius: 8px; font-weight: 600; transition: background 0.3s ease; }
        .btn:hover { background: #5a6fd8; }
        .status { background: #e8f5e8; border: 1px solid #4caf50; border-radius: 8px; padding: 20px; margin-bottom: 30px; }
        .status h3 { color: #2e7d32; margin-bottom: 10px; }
        .api-section { background: white; border-radius: 12px; padding: 30px; margin-bottom: 30px; }
        .api-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; }
        .api-link { display: block; background: #f8f9fa; border: 1px solid #dee2e6; padding: 15px; text-decoration: none; color: #495057; border-radius: 8px; text-align: center; transition: all 0.3s ease; }
        .api-link:hover { background: #667eea; color: white; border-color: #667eea; }
        .features { background: white; border-radius: 12px; padding: 30px; }
        .feature-list { list-style: none; }
        .feature-list li { padding: 10px 0; border-bottom: 1px solid #eee; }
        .feature-list li:last-child { border-bottom: none; }
        .feature-list li::before { content: "✅ "; margin-right: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🤖 OpenAI Realtime + Twilio</h1>
            <p>Voice AI Demo Application</p>
        </div>
        
        <div class="status">
            <h3>✅ System Status: Online & Ready</h3>
            <p>Your OpenAI Realtime API with Twilio integration is running successfully! All backend services are operational.</p>
        </div>
        
        <div class="grid">
            <div class="card">
                <h3>🎤 Voice Features</h3>
                <p>Real-time voice conversations with AI agents powered by OpenAI's Realtime API and Twilio Voice.</p>
                <a href="/tools" class="btn">View Available Tools</a>
            </div>
            
            <div class="card">
                <h3>🔧 AI Tools</h3>
                <p>Multiple AI tools including weather, knowledge base, customer lookup, and meeting scheduling.</p>
                <a href="/tools" class="btn">Explore Tools</a>
            </div>
            
            <div class="card">
                <h3>📱 Twilio Integration</h3>
                <p>Seamless phone integration for voice calls with AI agents. Ready for production use.</p>
                <a href="/public-url" class="btn">Get Public URL</a>
            </div>
        </div>
        
        <div class="api-section">
            <h3>🔗 API Endpoints</h3>
            <div class="api-grid">
                <a href="/tools" class="api-link">Available Tools</a>
                <a href="/health" class="api-link">Health Check</a>
                <a href="/public-url" class="api-link">Public URL</a>
                <a href="/status" class="api-link">System Status</a>
                <a href="/twiml" class="api-link">Twilio TwiML</a>
                <a href="/call" class="api-link">WebSocket Call</a>
            </div>
        </div>
        
        <div class="features">
            <h3>🚀 Available Features</h3>
            <ul class="feature-list">
                <li>Real-time voice conversations with AI</li>
                <li>Twilio phone integration</li>
                <li>WebSocket-based communication</li>
                <li>PostgreSQL database</li>
                <li>Multiple AI tools (weather, knowledge base, customer lookup, meeting scheduling)</li>
                <li>Production-ready deployment</li>
                <li>Docker containerization</li>
                <li>Nginx reverse proxy</li>
            </ul>
        </div>
    </div>
</body>
</html>';
            add_header Content-Type text/html;
        }

        # Static files caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            proxy_pass http://webapp;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Cache static files
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }
}
